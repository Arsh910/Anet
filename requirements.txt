# Web dashboard (server.py)
fastapi
uvicorn[standard]
pyyaml

# Core runtime
openai
anthropic
python-dotenv
httpx
rich
prompt_toolkit

# LangGraph orchestration
langgraph
langchain-core

# Vertex AI (optional — only needed for provider='vertex_google' or 'vertex_claude')
google-auth
google-auth-httplib2

# Desktop automation (open_app tool) — Windows only
pywinauto
pyautogui
Pillow
